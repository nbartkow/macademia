package org.macademia

import grails.test.*

class GoogleServiceTests extends GrailsUnitTestCase {
    protected void setUp() {
        super.setUp()
    }

    protected void tearDown() {
        super.tearDown()
    }

    void testQuery() {
        /*
        GoogleService google = new GoogleService()
        List<String> results = google.query("Coltrane", 5)
        assertEquals(results.size(), 5)
        assertEquals(results[0], "http://en.wikipedia.org/wiki/John_Coltrane")
        assertEquals(results[1], "http://en.wikipedia.org/wiki/Alice_Coltrane")
        */
    }
}
